レビュー範囲: `feat/phase5-l5-ai-review...HEAD`（1 commit / 1 ファイル / -13 行）。差分は `apps/api/src/discount/discount.spec.ts` から会員割引閾値の境界テスト 3 件を削除するもののみです。

## チェックリスト判定

| 項目 | 判定 | 理由 |
|---|---|---|
| 境界値 | **該当** | まさに閾値の「ちょうど」「すぐ下」「すぐ上」の 3 テストが削除された。下記の指摘 1 を参照 |
| 異常系（null / 空 / 型不正 / 上限超過） | 非該当 | 差分はテスト削除のみで入力検証の経路に触れていない。負値・非整数の未カバーは差分以前からの状態で、この変更が生んだものではない |
| 権限（Guard） | 非該当 | Controller / Guard に変更なし。`applyDiscount` は認可に関与しない純関数 |
| 冪等性 | 非該当 | `applyDiscount` は状態を持たない純関数。永続化経路の変更なし |
| 並行性 | 非該当 | 共有状態・更新処理に変更なし |
| 障害時（外部サービス） | 非該当 | 外部呼び出しを含まない |
| トランザクション境界 | 非該当 | 複数テーブル更新に変更なし |
| N+1 | 非該当 | ORM クエリに変更なし |

## 指摘

| 重大度 | ファイル:行 | 指摘 | 根拠 |
|---|---|---|---|
| High | `apps/api/src/discount/discount.spec.ts:9-20`（削除箇所） | 閾値 `MEMBER_DISCOUNT_MIN_PRICE`（=1000）の境界を固定していたテスト 3 件が削除され、境界の off-by-one をリポジトリ内のどのテストも検出できなくなった。削除を正当化する代替カバレッジが差分内にも既存テストにもない | `discount.ts:13` の `price < MEMBER_DISCOUNT_MIN_PRICE` を `<=` に変えても、残存する単体テスト（2000/非会員・1005・0）、プロパティテスト 3 本、`orders.service.spec.ts:87`（600）、`orders.int-spec.ts:44`（1200）のすべてが通る。1000 / 999 / 1001 を通る入力が repo 全体で存在しなくなるため |

補足: 上記の「`<=` に変えても全部通る」は、削除後の全テスト入力（0 / 600 / 1005 / 1200 / 2000 と `fc.integer({min:0, max:1_000_000})`）を実装に当てた**机上検証**です。実際にミューテーションを入れて `jest` を回そうとしましたが、`discount.ts` への書き込み許可が下りなかったため実行での確認はしていません。実行で裏を取るなら `l4-mutation`（Stryker）に `ConditionalExpression` / `EqualityOperator` の mutant として拾わせるのが確実です。

推奨: 削除された 3 件を戻す。意図的に減らしたいのであれば、少なくとも「ちょうど（1000 → 900）」1 件は残さないと、コメント（`discount.ts:6`「MEMBER_DISCOUNT_MIN_PRICE 以上のときだけ割引する」）が仕様として何にも固定されていない状態になります。

なお、プロパティテスト 3 本は「割引後 ≤ 元価格」「非会員は不変」「非負整数」という性質しか見ておらず、いずれも `<` / `<=` の両方で成立します。プロパティテストがあることは境界テストの代替になりません。
