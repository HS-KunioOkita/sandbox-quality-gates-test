`feat/phase5-l5-ai-review...HEAD` の差分をレビューしました。差分は 1 ファイル・13 行削除のみです。

## 差分の内容

`apps/api/src/discount/discount.spec.ts` から、会員割引の閾値（`MEMBER_DISCOUNT_MIN_PRICE` = 1000）に関する 3 テスト（ちょうど / すぐ下 / すぐ上）と、そのための import が削除されています。実装（`discount.ts`）は変更されていません。

## チェックリスト

| 項目 | 判定 | 理由 |
|---|---|---|
| 境界値 | **該当** | 削除された 3 件がまさに「ちょうど上・ちょうど・すぐ下」そのもの。削除後、閾値を固定するテストは 1 件も残っていない（下表 #1） |
| 異常系（null / 空 / 型不正 / 上限超過） | 非該当 | 差分は異常系テストに触れていない。`0 円` テストと property test（`fc.integer({min:0, max:1_000_000})`）は残存。負値・非整数・上限超過のテストは差分前から存在せず、この差分が持ち込んだ問題ではない |
| 権限（Guard） | 非該当 | Controller・Guard・認可経路に差分なし。`applyDiscount` は純関数 |
| 冪等性 | 非該当 | 副作用のない純関数のテストのみ。リクエスト処理に差分なし |
| 並行性 | 非該当 | 共有状態・更新処理に差分なし |
| 障害時（外部サービス） | 非該当 | 外部呼び出しを含まない |
| トランザクション境界 | 非該当 | DB 更新に差分なし |
| N+1 | 非該当 | ORM クエリに差分なし |

## 指摘

| 重大度 | ファイル:行 | 指摘 | 根拠 |
|---|---|---|---|
| **高** | `apps/api/src/discount/discount.spec.ts:8` | 会員割引の閾値（1000 円）を固定する境界値テストが 3 件とも削除され、代替がない。`discount.ts:13` の `price < MEMBER_DISCOUNT_MIN_PRICE` は、この差分適用後は**どのテストからも検証されない** | 残存テストは `(2000, false)`／`(1005, true)`／`(0, true)` の 3 件と property test 3 件のみ。(a) 比較演算子を `<` → `<=` に変えても、1005 も 0 も 2000 も判定が変わらないため全テストが通る。(b) `MEMBER_DISCOUNT_MIN_PRICE` を 0〜1005 のどの値に変えても全テストが通る（1005 のテストだけが上側を 1005 に緩く抑えている）。つまり閾値の位置も境界の開閉も未固定 |
| 中 | `apps/api/src/discount/discount.spec.ts:20-56` | property test が境界を補えていない点は明示しておく価値がある。3 つの性質（割引後 ≤ 元価格／非会員は不変／非負整数）はいずれも閾値の位置に依存しないため、境界値テストの代替にならない | 上記 (a)(b) の変異はいずれも 3 性質を保つ。`fc.integer` が 1000 近傍を引いても不変条件は成立するため失敗しない |

補足: 上記 (a)(b) は差分と実装のコード読解による導出で、この場では実際に実装を変異させて Jest を走らせてはいません（作業ツリーを汚さないため）。検証する場合は `discount.ts:13` の `<` を `<=` に変えて `pnpm --filter api test` が緑のままであることを確認するのが最短です。

なお import 削除（`MEMBER_DISCOUNT_MIN_PRICE`）は 3 テスト削除に伴う整合的な変更で、単体では指摘対象外です（未使用 import は lint が機械検出する範囲）。
