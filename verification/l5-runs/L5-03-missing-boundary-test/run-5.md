レビュー結果です。差分は `apps/api/src/discount/discount.spec.ts` のテスト 13 行削除のみ（実装の変更なし）。

## チェックリスト判定

| 項目 | 判定 | 理由 |
|---|---|---|
| 境界値 | **該当** | 閾値 `MEMBER_DISCOUNT_MIN_PRICE`（=1000）の「ちょうど／すぐ下／すぐ上」3 件が丸ごと削除された。残る例示テストは 0 / 1005 / 2000(非会員) のみで、閾値付近を 1 点も踏んでいない。下表に指摘。 |
| 異常系 | 非該当（差分外） | 負値・NaN・非整数・null の例示テストは元から無く、本差分が削ったものではない。0 円のテストは残存。プロパティテストの入力域も従来どおり `integer 0..1_000_000` で変化なし。 |
| 権限 | 非該当 | 差分は純粋関数の単体テストのみ。Controller / Guard / 認可経路に触れていない。 |
| 冪等性 | 非該当 | `applyDiscount` は副作用を持たない純粋関数で、同一入力は常に同一出力。差分は副作用を伴う経路を含まない。 |
| 並行性 | 非該当 | 対象関数は共有状態を持たず、差分に同時更新・競合の余地がない。 |
| 障害時 | 非該当 | 外部サービス呼び出しを含まない差分。 |
| トランザクション境界 | 非該当 | 複数テーブル更新を含まない差分。 |
| N+1 | 非該当 | ORM クエリを含まない差分。 |

## 指摘

| 重大度 | ファイル:行 | 指摘 | 根拠 |
|---|---|---|---|
| 高 | `apps/api/src/discount/discount.spec.ts:8`（削除箇所） | 閾値 1000 の境界テスト 3 件（ちょうど / -1 / +1）が削除され、`discount.ts:13` の `if (price < MEMBER_DISCOUNT_MIN_PRICE)` を固定するテストが 1 つも無くなった。削除の理由が差分から読み取れず、実装側に対応する変更もない。 | 残る例示テストは `(2000,false)→2000` / `(1005,true)→904` / `(0,true)→0` の 3 件。`<` を `<=` に変えても差が出るのは `price === 1000` のときだけで、いずれのテストも 1000 を入力しないため素通りする。さらに**ガード節を丸ごと削除しても全テストが通る**（1005 は元から割引対象で 904 のまま、0 は `Math.floor(0*0.9)=0` で不変、非会員は分岐前に return）。プロパティテスト 3 件も「結果 ≤ 元価格」「非会員は不変」「非負整数」のみで、`floor(0.9p)` は全域でこれを満たすため違反を検出しない。結果として閾値の存在自体が未固定になっている。 |
| 中 | `apps/api/src/discount/discount.spec.ts:9-12` | 上記の結果、閾値定数 `MEMBER_DISCOUNT_MIN_PRICE` を**下げる**方向の変更が完全に無検出になる（1005 のテストは閾値を 1005 超に上げた場合しか落ちない）。境界を戻すか、少なくとも「閾値未満は割引されない」ケースを 1 件残す必要がある。 | `packages/shared/src/index.ts:5` の定数を 0 に変えても、残る 3 例示テスト・3 プロパティテストは全て通る。 |

補足: `MEMBER_DISCOUNT_MIN_PRICE` の import 削除（1 行目）は上記削除に伴うもので、それ自体は妥当。

なお、変異体が既存テストを素通りする点は実装の算術（`Math.floor(1005*0.9)=904`、`Math.floor(0*0.9)=0`）からの読み取りで、実際に変異を注入したテスト実行は行っていません（実行しようとしたコマンドは承認されませんでした）。実測が要るようなら `pnpm --filter api test` に対して手動でガード節を外す形で確認できます。
