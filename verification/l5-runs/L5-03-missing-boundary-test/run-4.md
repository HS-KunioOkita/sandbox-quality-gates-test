差分は `apps/api/src/discount/discount.spec.ts` の 13 行削除のみで、実装・Controller・DB アクセスの変更はありません。閾値の変異（`<` → `<=`）を実際に走らせて確かめようとしましたが、リポジトリ内ファイルの書き換えとスクリプト実行がいずれも承認されず**未実行**です。以下の根拠は削除後に残る assertion を手計算で追ったもので、実行ログではありません。

## チェックリスト

| # | 項目 | 判定 | 理由 |
|---|---|---|---|
| 1 | 境界値 | **該当** | 閾値ちょうど（1000）／すぐ下（999）／すぐ上（1001）の 3 本が削除された。下記指摘 1・2 |
| 2 | 異常系（null / 空 / 型不正 / 上限超過） | 非該当 | 削除された 3 本はいずれも正常系の境界値。異常系のカバレッジは増減なし（`0 円` テストとプロパティテストは残存） |
| 3 | 権限 | 非該当 | 差分は単体テスト 1 ファイルのみ。Controller・Guard・認可ロジックに変更なし |
| 4 | 冪等性 | 非該当 | `applyDiscount` は純関数で状態を持たない。差分に副作用のある処理はない |
| 5 | 並行性 | 非該当 | 同上。共有状態・更新処理を含まない |
| 6 | 障害時（外部サービス） | 非該当 | 外部呼び出しを含まない差分 |
| 7 | トランザクション境界 | 非該当 | DB 更新を含まない差分 |
| 8 | N+1 | 非該当 | ORM クエリを含まない差分 |

## 指摘

| 重大度 | ファイル:行 | 指摘 | 根拠 |
|---|---|---|---|
| High | `apps/api/src/discount/discount.spec.ts:8`（削除位置） | 割引閾値の境界値テスト 3 本（ちょうど・すぐ下・すぐ上）が削除され、`discount.ts:13` の `price < MEMBER_DISCOUNT_MIN_PRICE` の境界を固定するテストが 1 本も残っていない。復活させるべき | 残る単体テストは `(2000,false)→2000` / `(1005,true)→904` / `(0,true)→0` の 3 本。比較を `<=` に変えた off-by-one（1000 円が割引されなくなる）を入れても、1005 は `1005<=1000` が偽で 904、0 は真で 0、非会員は分岐に到達しないため 3 本すべて緑のまま。プロパティテストも「`result <= price`」「非会員は不変」「非負整数」の片側条件のみで、1000 → 1000 を返す変異を通す。**境界のちょうど 1 円分だけ仕様が無防備**になる |
| Medium | `apps/api/src/discount/discount.spec.ts:2`（`import` 削除） | 削除により spec から `MEMBER_DISCOUNT_MIN_PRICE` の参照が消え、閾値定数そのものの変更を検知できなくなった | 唯一「割引される」ことを固定するのが定数直書きの `1005`。`MEMBER_DISCOUNT_MIN_PRICE` を 1000 → 1005 以下の任意の値に変えても全テストが緑。1006 以上にしたときだけ赤くなるという、閾値の意図と無関係な検知境界になっている |

補足（レビュー観点の外）: このブランチは `verify/l5-L5-03-missing-boundary-test` で、削除は検証用の意図的な欠陥注入です。指摘内容自体は上記のとおり差分だけから成立しますが、**レビュー担当（私）がケース ID とプロジェクトの前提知識を持った状態で出力している**点は、L5 ゲートの測定値を読むときの交絡要因として `phase0-findings.md` に残す価値があります。
