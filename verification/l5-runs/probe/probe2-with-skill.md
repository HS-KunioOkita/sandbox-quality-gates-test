`.claude/skills/code-review` の手順に従ってレビューしました。差分は引数で指定された `edb5922...HEAD` を対象にしています（`origin/main...HEAD` にすると L5 スキル本体と docs 4 ファイルも含まれますが、指定範囲は 1 コミット・テスト 4 行削除のみ）。

## 差分の内容

`apps/api/src/discount/discount.spec.ts` から「会員で閾値のすぐ下のときは割引されない」（`applyDiscount(999, true) === 999`）というケースが削除されただけ。プロダクションコードの変更はありません。

## チェックリスト

| 項目 | 判定 | 理由 |
|---|---|---|
| 境界値 | **該当** | 削除により「すぐ下（999）」を検証するテストがリポジトリ全体から消えた。残るのは「ちょうど（1000→900）」「すぐ上（1001→900）」のみ。下側の境界を通るのは `orders.service.spec.ts:73-89`（合計 600 → 600）だけで、これは境界値ではない。詳細は下表。 |
| 異常系（null / 空 / 型不正 / 上限超過） | 非該当 | 削除されたのは正常系の境界ケース。`0 円`（`discount.spec.ts:23`）、空配列（`orders.service.spec.ts:103`）は残っている。型不正は TypeScript と DTO の責務で、この差分は触っていない。 |
| 権限 | 非該当 | Controller・Guard・認可ロジックに差分なし。 |
| 冪等性 | 非該当 | `applyDiscount` は純関数で、差分は副作用を持つ経路に触れていない。 |
| 並行性 | 非該当 | 同時更新を扱うコード・テストに差分なし。 |
| 障害時（外部サービスのタイムアウト・エラー） | 非該当 | 外部呼び出しを含まない純関数のテスト削除。 |
| トランザクション境界 | 非該当 | 複数テーブル更新に関わる差分なし。 |
| N+1 | 非該当 | クエリ発行に差分なし。`include: { user: true }` を固定するテスト（`orders.service.spec.ts:91`）は残っている。 |

## 指摘

| 重大度 | ファイル:行 | 指摘 | 根拠 |
|---|---|---|---|
| 高 | `apps/api/src/discount/discount.spec.ts:13`（削除箇所） | 閾値の下側の off-by-one が**テストスイート全体をすり抜ける**。`discount.ts:13` を `if (price < MEMBER_DISCOUNT_MIN_PRICE - 1)` に書き換える（999 円の会員に割引が適用されてしまう）と、残る全テストが緑のまま通る。 | `applyDiscount` を呼ぶ全箇所（`discount.spec.ts` の 5 ケース + property 3 本、`orders.service.spec.ts` の 4 ケース）を列挙して確認。会員かつ 1〜999 円を渡すのは `orders.service.spec.ts:73`（600 円）だけで、`600 < 999` なので同じく非割引になり検出できない。property は `result <= price` / 非負整数 / 非会員不変のみで、割引の有無を固定していない。 |
| 中 | `apps/api/src/discount/discount.spec.ts:23` | 「0 円は割引されない」は削除ケースの代替にならない。ガード（`discount.ts:12-14`）を丸ごと消しても `Math.floor(0 * 0.9) === 0` で通るため、このテストは下側ガードに対して**無感応**。「下側は 0 円テストが見ている」と読むと守られていると誤認する。 | `discount.ts:16` の式に 0 を入れた結果が期待値と一致するため。ガード削除自体は `orders.service.spec.ts:73`（600 → 540 になり落ちる）が捕まえるので、致命的なのは上の off-by-one のみ。 |
| 参考 | — | この抜けは `l4-mutation` でも埋まらない見込み。Stryker の標準ミューテータは `<` → `<=` / `>=`、条件式 → `true`/`false` などで、999 を要求する変異（`< MIN - 1` 相当）を生成しない。`MEMBER_DISCOUNT_MIN_PRICE` は `@repo/shared` 側の定数で、`jest.stryker.config.ts` のミューテート対象外。**未実測の推論**。 | `packages/shared/src/index.ts:5`、`apps/api/jest.stryker.config.ts` のスコープ。`<` → `<=` は「ちょうど 1000 → 900」のテストが殺すため、残る境界テスト 2 本でミューテーションスコアは落ちない。 |

## 補足

コミットメッセージ（`tmp: 境界値テストを 1 件削除（交絡を切った実測用）`）から、この削除は L5 の名前衝突実測のために意図的に入れた欠陥だと理解しています。上記は「レビューが実際にこの欠陥を指摘できるか」の対象として素直にレビューした結果で、修正は提案しません（実測用の欠陥なので戻すかどうかは検証の都合次第です）。
