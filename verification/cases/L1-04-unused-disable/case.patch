diff --git a/apps/api/src/discount/discount.ts b/apps/api/src/discount/discount.ts
index cac33e0..622d355 100644
--- a/apps/api/src/discount/discount.ts
+++ b/apps/api/src/discount/discount.ts
@@ -6,6 +6,7 @@ import { MEMBER_DISCOUNT_MIN_PRICE, MEMBER_DISCOUNT_RATE } from '@repo/shared';
  * 会員であり、かつ price が MEMBER_DISCOUNT_MIN_PRICE 以上のときだけ割引する。
  * 割引後の端数は切り捨てる。
  */
+// eslint-disable-next-line eqeqeq -- 実際には == を使っていないので効かない
 export function applyDiscount(price: number, isMember: boolean): number {
   if (!isMember) {
     return price;
